# Cinematic AI portfolio

User approved the Systems Architect direction and delegated implementation decisions, requesting an AI touch and a separate pushed branch.

## Experience
Obsidian and warm ivory, mint accents, oversized editorial name, a custom SVG neural core, and a sticky scroll-driven architecture scene. The three layers describe distributed systems, security, and applied AI. Project gallery uses large alternating compositions, followed by experience, skills, terminal, scripted Q&A, and contact. All existing content and interactions remain accessible.

## Implementation boundaries
Retain Next.js, React, typed content, project routes, semantic landmarks, and theme support. Use deterministic SVG and CSS rather than asset downloads or a new 3D dependency. Native scrolling drives transforms through one requestAnimationFrame callback; no scroll interception. Static content renders before JavaScript. Mobile and reduced motion display the three disciplines in normal flow. Explicit pause control disables decorative and scroll motion. No live AI backend or invented professional claims.

## Acceptance
Desktop/mobile layouts have no horizontal overflow. All projects, modal keyboard behavior, terminal, Q&A, navigation, resume, and both themes work. Reduced motion and disabled JavaScript retain content. Run lint, typecheck, unit tests, production build, browser journeys, and inspect desktop/mobile screenshots before commit and push.

## Revision: continuous chapter experience
The user requested useful scrolling from the top, with each scroll revealing a next page. Replace the isolated architecture pin with native desktop scroll snapping throughout the portfolio. A full-screen introduction leads to philosophy, six individually composed project chapters with real impact, experience, capabilities, terminal, Q&A, and contact. A persistent chapter navigator tracks progress and supports direct links, next-chapter navigation, keyboard access, and back-to-top. Mobile and reduced motion use natural flow. Transforms apply only to decorative art and remain optional. Keep all functional features and existing truthful content. Continue on the requested feature branch.

## Approved motion revision — September 2026

The user approved the “Inside the Machine” visual concept after asking for continuous scroll-driven scene motion inspired by Logartis, with a technology theme and no free-flight controls.

The homepage now uses a shared, perspective-projected machine scene. Native document scrolling opens a processor into system layers, rearranges its modules into an AI workflow, and transforms them into project-specific compositions. Scroll reversal reproduces the same scene exactly. Text panels pin only if their measured height fits between the header and chapter bar; larger text and short windows retain normal document flow. Direct section links, all six project dialogs, the terminal, scripted guide, resume, and contact links remain available.

The scene is decorative and contains no essential content. Server-rendered illustrations remain the fallback when canvas is unavailable or JavaScript is disabled. Reduced-motion visitors see discrete scene states with unpinned reading sections. The motion button freezes geometry. Canvas renders only in response to scroll, resize, theme, or motion changes; no animation library, external asset, or continuously running animation loop was added.

Verification covers the first scroll changing the rendered canvas, exact reversal, direct jumps, pause/resume, reduced motion, no-JavaScript detail links, enlarged-text panel overflow, existing portfolio interactions, lint, TypeScript, unit tests, and the production build.
